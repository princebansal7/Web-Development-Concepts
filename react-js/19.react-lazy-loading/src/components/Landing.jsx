export default function Landing() {
    return (
        <div>
            <h2>Landing</h2>
        </div>
    );
}

// Changing exports to default, as for lazy loading we will
// need to import this component in different way
